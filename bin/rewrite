#!/usr/bin/env node

const { content, writeContent } = require('../src/content');

Object.entries(content).forEach(([key, doc]) => {
  if (doc.queue) {
    doc.queue = doc.queue.filter((v,i,a) => a.indexOf(v) === i)
  }
})

writeContent()
